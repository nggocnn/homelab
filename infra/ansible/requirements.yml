---
# Installed into ./collections, ahead of the apt package's bundled copies.
collections:
  - name: ansible.posix        # authorized_key
    version: ">=2.2.2"         # apt ships 2.1.0: to_native deprecation warning
  - name: community.proxmox    # proxmox_pct_remote connection (bastion)
  - name: community.crypto     # openssh_keypair (bastion)
