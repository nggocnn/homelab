---
# The VIP first, the gateway behind it. This is an access path: if the resolver pair
# is down and the container restarts, it still has to resolve its control plane to
# reconnect. Filtering is not the point for these hostnames.
lxc_resolvers: "{{ dns_vip }} {{ pve_gateway }}"
